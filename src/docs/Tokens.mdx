import { Meta } from '@storybook/addon-docs/blocks';
import { TokenTable } from './TokenTable';

<Meta title="Design System/Tokens" />

# Tokens

The source of truth for tokens is `tokens/theme.json`.

Style Dictionary generates the CSS custom properties consumed by the components.

<TokenTable />
